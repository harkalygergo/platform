security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # Use an entity user provider so Doctrine users are loaded from the database.
    providers:
        app_user_provider:
            entity:
                class: App\Entity\Platform\User
                # property used to load the user (username field)
                property: username
        # keep the in-memory provider for tests or other uses
        users_in_memory: { memory: null }
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider

            # enable form login so the /login form is processed by the firewall
            form_login:
                login_path: security_login
                check_path: security_login
                enable_csrf: true
                csrf_token_id: authenticate
                username_parameter: _username
                password_parameter: _password
                default_target_path: homepage

            # basic logout configuration (optional but handy)
            logout:
                path: /logout
                invalidate_session: true
                target: security_login

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
    role_hierarchy:
        ROLE_SUPERADMIN: [ROLE_ADMIN, ROLE_USER]
        ROLE_ADMIN: [ROLE_USER]

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
