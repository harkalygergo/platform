{% for menu in menus %}
    {% if menu.parent is null %}
        {% if menu.hasChildren() %}
            {# Parent menu with children - render as dropdown #}
            <li class="dropdown">
                <a class="nav-link px-2 text-white dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    {{ menu.title }}
                </a>
                <ul class="dropdown-menu">
                    {% for child in menu.children %}
                        {% if child.status %}
                            <li><a class="dropdown-item" href="{{ child.slug }}">{{ child.title }}</a></li>
                        {% endif %}
                    {% endfor %}
                </ul>
            </li>
        {% else %}
            {# Simple menu item without children #}
            <li>
                <a class="nav-link px-2 text-white" aria-current="page" href="{{ menu.slug }}">{{ menu.title }}</a>
            </li>
        {% endif %}
    {% endif %}
{% endfor %}
